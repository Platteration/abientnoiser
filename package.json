{
  "name": "ambient-noiser",
  "version": "1.0.0",
  "description": "Hour-long looping generative ambient / lo-fi soundscapes with mood changes, built on the Web Audio API.",
  "private": true,
  "scripts": {
    "start": "node scripts/serve.js",
    "test": "node --test test/*.test.js",
    "test:browser": "node test/browser.smoke.mjs",
    "test:musical": "node test/musical.smoke.mjs",
    "test:all": "npm test && npm run test:musical && npm run test:textures && npm run test:browser",
    "test:textures": "node test/texture.smoke.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
